import React from 'react'
//import PropTypes from 'prop-types'

const SearchPresentationalComponent = () => (
  <form>
    <input placeholder="search"></input>
    <button>Find</button>
  </form>
)

export default SearchPresentationalComponent
